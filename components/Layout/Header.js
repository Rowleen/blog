import styles from 'styles/components/header.module.sass'

const Header = () => {
  return (
    <header className={styles.header}>
      <div className={styles.header__content}>Header content</div>
    </header>
  )
}

export default Header
